# Apache設定（publicディレクトリ用）
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /shiori/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>

